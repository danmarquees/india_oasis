# India Oasis - cPanel Compatible Requirements
# Simplified dependencies for GoDaddy cPanel shared hosting

# Core Django
Django==5.2.3
asgiref==3.8.1
sqlparse==0.5.3

# MySQL Database (instead of PostgreSQL)
mysqlclient==2.2.4

# Environment and Configuration
django-environ==0.11.2

# Image Processing
Pillow==11.2.1

# Payment Processing
mercadopago==2.2.0

# HTTP Client
requests==2.31.0

# Email Backend
django-email-backend==1.1.0

# Security (basic only)
django-cors-headers==4.3.1

# Utilities
python-dateutil==2.8.2
pytz==2023.3

# File handling
python-magic==0.4.27

# JSON logging
python-json-logger==2.0.7

# Form handling
django-crispy-forms==2.0

# Text processing
unidecode==1.3.7

# Development dependencies (optional - uncomment for local development)
# django-debug-toolbar==4.2.0
# django-extensions==3.2.3

# Note: The following dependencies have been REMOVED for cPanel compatibility:
# - psycopg2-binary (PostgreSQL - replaced with mysqlclient)
# - redis and django-redis (Redis caching - replaced with file-based cache)
# - celery, django-celery-beat, django-celery-results (Background tasks - removed)
# - gunicorn (WSGI server - replaced with Passenger WSGI)
# - whitenoise (Static files middleware - not needed in cPanel)
# - django-storages (Cloud storage - using local file storage)
# - sentry-sdk (Error tracking - simplified logging)
# - django-health-check (Health monitoring - removed)
# - django-ratelimit (Rate limiting - disabled for cPanel)
# - django-admin-interface (Complex admin - using default Django admin)
# - django-colorfield (Admin enhancement - removed)
